#!/usr/bin/env zsh
# [MISE] description="nvim editor"
# [MISE] dir="{{cwd}}"

# neovim editor

if [ $# -gt 0 ]; then
  print -P "%F{magenta}nvim $@ %f"
  print
  nvim "$@"
else
  print -P "%F{magenta}nvim . %f"
  print
  nvim .
fi
