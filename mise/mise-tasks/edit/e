#!/usr/bin/env zsh
# [MISE] description="default editor"
# [MISE] dir="{{cwd}}"

# default editor

if [ $# -gt 0 ]; then
  print -P "%F{magenta}$EDITOR $@ %f"
  print
  $EDITOR "$@"
else
  print -P "%F{magenta}$EDITOR . %f"
  print
  $EDITOR .
fi
